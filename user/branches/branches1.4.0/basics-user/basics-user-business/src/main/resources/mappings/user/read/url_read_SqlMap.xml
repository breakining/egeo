<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.basics.user.dao.read.UrlReadDAO">
	<resultMap type="com.egeo.basics.user.po.UrlPO" id="urlMap">
		<result property="id" column="id" />
		<result property="url" column="url" />
		<result property="name" column="name" />
		<result property="description" column="description" />
		<result property="platformId" column="platform_id" />
		<result property="createTime" column="create_time" />
		<result property="updateTime" column="update_time" />
	</resultMap>

	<sql id="urlColumns">
		id,
		url,
		name,
		description,
		platform_id,
		create_time,
		update_time
	</sql>

	<select id="findById" parameterType="com.egeo.basics.user.po.UrlPO" resultMap="urlMap">
		SELECT
		<include refid="urlColumns" />
		FROM u_url
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="urlMap">
		SELECT
		<include refid="urlColumns" />
		FROM u_url
		<where>
			<if test="po.id != null">
				and id = #{po.id}
			</if>
			<if test="po.url != null">
				and url = #{po.url}
			</if>
			<if test="po.name != null">
				and name = #{po.name}
			</if>
			<if test="po.description != null">
				and description = #{po.description}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM u_url
		<where>
			<if test="po.id != null">
				and id = #{po.id}
			</if>
			<if test="po.url != null">
				and url = #{po.url}
			</if>
			<if test="po.name != null">
				and name = #{po.name}
			</if>
			<if test="po.description != null">
				and description = #{po.description}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="urlMap">
		SELECT
		<include refid="urlColumns" />
		FROM u_url
		<where>
		    <if test="po.id != null">
				and id = #{po.id}
			</if>
			<if test="po.url != null">
				and url = #{po.url}
			</if>
			<if test="po.name != null">
				and name = #{po.name}
			</if>
			<if test="po.description != null">
				and description = #{po.description}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
	<select id="getUrlListByUserId" parameterType="java.lang.Long" resultType="java.lang.String">
		SELECT DISTINCT u.url FROM u_url u
		LEFT JOIN u_role_url ru ON ru.url_id = u.id
		LEFT JOIN u_user_role ro  ON ro.role_id = ru.role_id
		where ro.user_id= #{userId}
	</select>	
	
	<select id="getUrlByRoleId" parameterType="java.lang.Long" resultMap="urlMap">
		SELECT DISTINCT u.id,u.url,u.name FROM u_url u
		LEFT JOIN u_role_url ru ON ru.url_id = u.id
		where ru.role_id= #{roleId}
	</select>
</mapper>
	