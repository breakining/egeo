<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.basics.user.dao.write.RoleMenuWriteDAO">
	<insert id="insert" parameterType="com.egeo.basics.user.po.RoleMenuPO">
		INSERT INTO u_role_menu(
		role_id,
		menu_id
		) VALUES (
		#{roleId},
		#{menuId}
		)
		<selectKey keyProperty="id" resultType="java.lang.Long">
			select LAST_INSERT_ID()
		</selectKey>
	</insert>

	<update id="update">
		UPDATE u_role_menu SET
		role_id = #{roleId},
		menu_id = #{menuId}
		WHERE id = #{id}
	</update>

	<delete id="delete">
		delete from u_role_menu where id = #{id}
	</delete>
</mapper>
	