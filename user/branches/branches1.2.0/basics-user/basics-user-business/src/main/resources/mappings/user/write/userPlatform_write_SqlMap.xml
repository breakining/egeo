<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.basics.user.dao.write.UserPlatformWriteDAO">
	<insert id="insert" parameterType="com.egeo.basics.user.po.UserPlatformPO">
		INSERT INTO u_user_platform(
		user_id,
		platform_id
		) VALUES (
		#{userId},
		#{platformId}
		)
		<selectKey keyProperty="id" resultType="java.lang.Long">
			select LAST_INSERT_ID()
		</selectKey>
	</insert>

	<update id="update">
		UPDATE u_user_platform SET
		user_id = #{userId},
		platform_id = #{platformId}
		WHERE id = #{id}
	</update>

	<delete id="delete">
		delete from u_user_platform where id = #{id}
	</delete>
</mapper>
	