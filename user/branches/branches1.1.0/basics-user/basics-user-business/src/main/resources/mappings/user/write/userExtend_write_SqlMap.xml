<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.basics.user.dao.write.UserExtendWriteDAO">
	<insert id="insert" parameterType="com.egeo.basics.user.po.UserExtendPO">
		INSERT INTO u_user_extend(
		sex,
		nickname,
		head_pic_url
		) VALUES (
		#{sex},
		#{nickname},
		#{headPicUrl}
		)
		<selectKey keyProperty="id" resultType="java.lang.String">
			select LAST_INSERT_ID()
		</selectKey>
	</insert>

	<update id="update">
		UPDATE u_user_extend SET
		sex = #{sex},
		nickname = #{nickname},
		head_pic_url = #{headPicUrl}
		WHERE id = #{id}
	</update>

	<delete id="delete">
		delete from u_user_extend where id = #{id}
	</delete>
</mapper>
	